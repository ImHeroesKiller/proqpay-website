"use client";import {useEffect,useState} from "react";import Link from "next/link";import {ArrowLeft,FileText,LogOut,RefreshCw} from "lucide-react";import {Button} from "@/components/ui/button";import {Badge} from "@/components/ui/badge";type Row=Record<string,unknown>;type Portal={application:Row;documents:Row[];activity:Row[]};
export function ClientPortalHome(){const [data,setData]=useState<Portal|null>(null);const [error,setError]=useState("");const load=async()=>{const r=await fetch("/api/payroll/client");if(r.status===401||r.status===403){window.location.href="/portal";return}const b=await r.json();if(!r.ok){setError(b.error||"Data tidak dapat dimuat");return}setData(b)};useEffect(()=>{void load()},[]);const logout=async()=>{await fetch("/api/payroll/logout",{method:"POST"});window.location.href="/portal"};if(!data)return <main className="container-pro py-20"><Button asChild variant="ghost"><Link href="/portal"><ArrowLeft/>Portal Login</Link></Button><p className="mt-8">{error||"Memuat dashboard client..."}</p></main>;const a=data.application,c=JSON.parse(String(a.company_json||"{}")),p=JSON.parse(String(a.payroll_json||"{}"));return <main className="min-h-screen bg-[#f4f6f8]"><header className="bg-[#0B1F33] text-white"><div className="container-pro flex flex-wrap items-center justify-between gap-4 py-10"><div><Button asChild variant="ghost" className="mb-3 text-white"><Link href="/"><ArrowLeft/>Website MSG</Link></Button><p className="text-xs font-bold uppercase tracking-wider text-orange">{String(a.registration_number)}</p><h1 className="mt-2 text-3xl font-bold">{c.legalName}</h1><p className="text-white/60">Client Portal</p></div><div className="flex gap-2"><Button onClick={load} className="bg-white/10 text-white"><RefreshCw/>Refresh</Button><Button onClick={logout} variant="ghost" className="text-white"><LogOut/>Logout</Button></div></div></header><div className="container-pro py-8"><div className="grid gap-4 md:grid-cols-4"><Stat label="Status" value={String(a.status).replaceAll("_"," ")}/><Stat label="Employees" value={String(p.employeeCount||"—")}/><Stat label="Monthly Payroll" value={`IDR ${Number(p.monthlyPayroll||0).toLocaleString("id-ID")}`}/><Stat label="Assessment" value={a.assessment_score==null?"In Review":`${a.assessment_score}/100`}/></div><div className="mt-6 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-bold">Documents</h2><Badge>{data.documents.length} files</Badge></div><div className="mt-4 space-y-3">{data.documents.map(d=><div key={String(d.id)} className="flex gap-3 rounded-xl bg-muted/50 p-4"><FileText className="text-[#0B3A6E]"/><div><strong>{String(d.original_filename)}</strong><p className="text-xs text-muted-foreground">{String(d.category)} · {String(d.document_type)}</p></div></div>)}</div></section><section className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Timeline</h2><div className="mt-4 space-y-4">{data.activity.map((x,i)=><div key={i} className="border-l-2 border-orange pl-4"><strong className="text-sm">{String(x.action).replaceAll("_"," ")}</strong><p className="text-xs text-muted-foreground">{new Date(String(x.created_at)).toLocaleString("id-ID")}</p></div>)}</div></section></div></div></main>}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-2xl border bg-white p-5"><p className="text-xs uppercase text-muted-foreground">{label}</p><p className="mt-2 text-lg font-bold">{value}</p></div>}
