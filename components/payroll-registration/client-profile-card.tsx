"use client";import {useEffect,useState} from "react";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Label} from "@/components/ui/label";
export function ClientProfileCard(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [status,setStatus]=useState("");useEffect(()=>{fetch("/api/payroll/profile").then(r=>r.json()).then(b=>{setName(b.profile?.display_name||"");setEmail(b.profile?.email||"")})},[]);const save=async()=>{const r=await fetch("/api/payroll/profile",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:name,email})});const b=await r.json();setStatus(r.ok?"Profil dan tujuan notifikasi tersimpan.":b.error)};return <section className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Profil & Notifikasi</h2><p className="mt-1 text-sm text-muted-foreground">Email ini menerima notifikasi saat admin mengirim pesan baru.</p><div className="mt-4 space-y-3"><div><Label>Nama</Label><Input className="mt-2" value={name} onChange={e=>setName(e.target.value)} placeholder="Nama PIC"/></div><div><Label>Email</Label><Input className="mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="pic@company.com"/></div><Button className="w-full" onClick={save}>Save Profile</Button>{status&&<p className="text-xs text-emerald-700">{status}</p>}</div></section>}
