import { NextResponse } from "next/server";
import { payrollWorker } from "@/lib/payroll/client";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!process.env.PAYROLL_ADMIN_KEY||request.headers.get("x-admin-key")!==process.env.PAYROLL_ADMIN_KEY)return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const response=await payrollWorker(`/admin/applications/${id}/assessment/decision`,{method:"POST",headers:{"content-type":"application/json"},body:await request.text()});return new NextResponse(response.body,{status:response.status,headers:{"content-type":"application/json"}})}
