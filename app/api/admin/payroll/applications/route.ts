import { NextResponse } from "next/server"; import { payrollWorker } from "@/lib/payroll/client";
export async function GET(request:Request){if(request.headers.get("x-admin-key")!==process.env.PAYROLL_ADMIN_KEY)return NextResponse.json({error:"Unauthorized"},{status:401});const response=await payrollWorker("/admin/applications");return new NextResponse(response.body,{status:response.status,headers:{"content-type":"application/json"}})}
