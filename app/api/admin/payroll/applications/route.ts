import {NextResponse} from "next/server";import {payrollWorker} from "@/lib/payroll/client";import {adminAuthorized} from "@/lib/payroll/auth";export async function GET(request:Request){if(!await adminAuthorized(request))return NextResponse.json({error:"Unauthorized"},{status:401});const response=await payrollWorker("/admin/applications");return new NextResponse(response.body,{status:response.status,headers:{"content-type":"application/json"}})}
