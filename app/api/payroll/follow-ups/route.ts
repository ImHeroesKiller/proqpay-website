import {NextResponse} from "next/server";import {payrollWorker} from "@/lib/payroll/client";import {portalToken} from "@/lib/payroll/auth";export async function POST(request:Request){const token=portalToken(request);if(!token)return NextResponse.json({error:"Unauthorized"},{status:401});const response=await payrollWorker("/portal/follow-ups",{method:"POST",headers:{"content-type":"application/json","x-portal-token":token},body:await request.text()});return new NextResponse(response.body,{status:response.status,headers:{"content-type":"application/json"}})}
