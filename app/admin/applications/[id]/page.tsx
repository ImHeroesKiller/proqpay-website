import {SubmissionDetail} from "@/components/payroll-registration/submission-detail";export const metadata={title:"Submission Detail | MSG",robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <SubmissionDetail id={id}/>}
